import { Trigger, Title, Description, Close } from '@radix-ui/react-dialog';

export const ModalTrigger = Trigger;

export const ModalTitle = Title;

export const ModalDescription = Description;

export const ModalClose = Close;
